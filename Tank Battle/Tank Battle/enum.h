#pragma once

enum direction{ up, down, left, right };
enum command{ moveup, movedown, moveleft, moveright, shoot_bullet};
enum from { fromplayer, fromenemy };
enum barrier_type{ steel, brick, grass, water};
enum item_type { bomb, star, timestop, protect, life };
enum effect_type{ blast_effect, born_effect, hit_effect};
enum enemyColor { white, green, yellow };
enum game_difficulty { easy, normal, hard };


